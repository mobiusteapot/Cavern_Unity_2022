using UnityEngine;
namespace ETC.KettleTools {
    public class ReadmePropertyDrawer : MonoBehaviour {
        // For drawers for file folder + displaying on the bottom of other components if in scene?
    }
}