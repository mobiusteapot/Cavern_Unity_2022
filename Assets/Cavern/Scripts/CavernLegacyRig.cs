using UnityEngine;

namespace ETC.CaveCavern
{
    public class CavernLegacyRig : MonoBehaviour
    {
        private CavernOutputSettings settings;
        // Currently empty but used to label parent of the cavern legacy rig
        private void Awake()
        {
          //  this.gameObject.ValidateIfEnabled();
        }
    }
}
